---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Button from "../../components/Button.astro";
import Container from "../../components/Container.astro";
import PremiumCard from "../../components/PremiumCard.astro";
import SectionHeader from "../../components/SectionHeader.astro";
import { compareMeta } from "../../content/compare-meta";
import { comparePages, type CompareSlug } from "../../content/compare-pages";

export function getStaticPaths() {
  return Object.keys(comparePages).map((slug) => ({
    params: { slug },
    props: { page: comparePages[slug as CompareSlug] }
  }));
}

const { page } = Astro.props as {
  page: (typeof comparePages)[CompareSlug];
};

const meta = compareMeta[Astro.params.slug as CompareSlug];
const alternativeName = page.alternative;
const verdictCards = [
  {
    title: `Where ${alternativeName} fits`,
    text: page.sections.alternative
  },
  {
    title: "Where EVENTS Gateway fits",
    text: page.sections.eventsgateway
  },
  {
    title: "Decision rule",
    text: page.sections.decision
  }
];

const renderScore = (value: number) => "●".repeat(value) + "○".repeat(5 - value);
---

<BaseLayout
  title={page.title}
  description={page.description}
  canonicalPath={`/compare/${Astro.params.slug}/`}
>
  <section class="section page-hero">
    <Container>
      <div class="stack">
        <span class="eyebrow">Comparison</span>
        <h1 class="page-title">{page.title}</h1>
        <p class="page-copy">{page.description}</p>
        <div class="pill-row">
          <span class="status-pill">{meta.category}</span>
          <span class="status-pill">{meta.bestFor}</span>
        </div>
        <div class="button-row">
          <Button href="/compare/" variant="secondary" icon="arrow-right">Back to comparisons</Button>
          <Button href="/install/" variant="primary" icon="download">Install EVENTS Gateway</Button>
          <Button href="/why-not-direct-pixels/" variant="secondary" icon="arrow-right">Why not direct pixels?</Button>
        </div>
      </div>
    </Container>
  </section>

  <section class="section section--tight">
    <Container>
      <SectionHeader
        eyebrow="Scorecard"
        title={`A fast read on ${alternativeName} vs EVENTS Gateway.`}
        description={`This scorecard highlights the practical trade-off between ${alternativeName} and EVENTS Gateway for signal quality, routing, and launch complexity.`}
      />
      <div class="card-grid">
        {
          meta.scorecard.map((item) => (
            <PremiumCard icon="layers" title={item.label} text={item.note} eyebrow="Scorecard">
              <div class="score-rail">
                <div class="score-rail__row">
                  <span>{alternativeName}</span>
                  <code>{renderScore(item.alternative)}</code>
                </div>
                <div class="score-rail__row">
                  <span>EVENTS Gateway</span>
                  <code>{renderScore(item.eventsgateway)}</code>
                </div>
              </div>
            </PremiumCard>
          ))
        }
      </div>
    </Container>
  </section>

  <section class="section section--tight">
    <Container>
      <SectionHeader
        eyebrow="Quick Verdict"
        title={page.winner}
        description={`Use this section to see where ${alternativeName} is still the better fit and where EVENTS Gateway creates a clearer advantage.`}
      />
      <div class="card-grid">
        {
          verdictCards.map((item) => (
            <PremiumCard icon="check" title={item.title} text={item.text} />
          ))
        }
      </div>
    </Container>
  </section>

  <section class="section">
    <Container>
      <SectionHeader
        eyebrow="Head-to-Head"
        title={`${alternativeName} vs EVENTS Gateway by buying criteria.`}
        description="The goal here is clarity for marketers, not a generic feature checklist."
      />
      <div class="surface-panel table-panel">
        <div class="surface-panel__inner">
          <div class="docs-table-wrap">
            <table class="docs-table">
              <thead>
                <tr>
                  <th>Dimension</th>
                  <th>{alternativeName}</th>
                  <th>EVENTS Gateway</th>
                </tr>
              </thead>
              <tbody>
                {
                  page.table.map((row) => (
                    <tr>
                      <td>{row[0]}</td>
                      <td>{row[1]}</td>
                      <td>{row[2]}</td>
                    </tr>
                  ))
                }
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </Container>
  </section>
</BaseLayout>

<style>
  .table-panel {
    overflow: hidden;
  }

  .docs-table-wrap {
    overflow-x: auto;
  }

  .docs-table {
    width: 100%;
    min-width: 760px;
    border-collapse: collapse;
  }

  .docs-table th,
  .docs-table td {
    padding: 0.95rem 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    text-align: left;
    vertical-align: top;
    line-height: 1.6;
  }

  .docs-table th {
    color: var(--color-subtle);
    font-size: 0.74rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .score-rail {
    display: grid;
    gap: 0.8rem;
  }

  .score-rail__row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    color: var(--color-muted);
  }

  .score-rail__row code {
    font-family: var(--font-mono);
    color: var(--color-text);
    letter-spacing: 0.2em;
    white-space: nowrap;
  }
</style>
