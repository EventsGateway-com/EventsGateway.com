export type IntegrationItem = {
  slug: string;
  label: string;
  title: string;
  summary: string;
  hero: string;
  repository: string;
  points: [string, string, string];
  useCases: [string, string, string];
};
